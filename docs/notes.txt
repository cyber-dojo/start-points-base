
exercises should not be deciding which visible_file to return.

New API?
o) names           (was start_points)
o) manifests       (new, returns everything, batch-method)
o) manifest(name)  (was manifest(display_name))

Use names(), manifest(name) for custom,languages
Use manifests() for exercises
